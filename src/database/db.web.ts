// Versao WEB - sem SQLite. As tabelas viram so um placeholder
// (a persistencia real e feita nos stores .web.ts em memoria)
export const initDatabase = () => {
    // no-op no web
}

// Objeto dummy so para satisfazer imports
export default {}
